bin/
# cdk/bin holds the CDK app entrypoint (source, not build output) — the bare
# bin/ rule above (Go build output) would otherwise swallow it silently.
!cdk/bin/
node_modules/
cdk.out/
*.log
.DS_Store
